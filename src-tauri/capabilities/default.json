{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the application window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
